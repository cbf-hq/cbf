[package]
name = "cbf-compositor"
version = "0.1.0-alpha.1"
edition.workspace = true
authors.workspace = true
description = "GUI composition integration layer for CBF desktop applications"
repository.workspace = true
keywords.workspace = true
license.workspace = true
readme.workspace = true

[features]
default = ["chrome", "winit"]
chrome = ["dep:cbf-chrome"]
winit = ["dep:winit"]

[dependencies]
cbf = { version = "0.1.0-alpha.1", path = "../cbf" }
cbf-chrome = { version = "0.1.0-alpha.1", path = "../cbf-chrome", optional = true }
raw-window-handle.workspace = true
thiserror.workspace = true
winit = { workspace = true, optional = true }
