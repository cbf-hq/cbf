# cbf-compositor

`cbf-compositor` is the GUI composition integration layer for CBF desktop applications.

It offers optional integration points for `cbf-chrome` and `winit`.

This crate is not part of the current public release set and remains under active development.

See the repository root README.md for current CBF status and supported runtime targets.
