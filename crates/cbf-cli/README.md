# cbf-cli

`cbf-cli` provides command-line tooling for CBF development workflows.

It is intended for local development and repository-oriented tasks around the CBF project.

CBF is currently in alpha. Commands, behavior, and output may still change.

See the repository root README.md for project context and the repository docs for contributor workflows.
