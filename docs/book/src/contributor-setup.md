# Contributor Setup

This chapter is for contributors building or modifying CBF locally.

It should cover:

- Rust workspace checks
- local Chromium and `cbf_bridge` builds
- `depot_tools` expectations
- how to choose the correct build path for Chromium-side work

Contributor setup should stay separate from user setup so beginners do not need to absorb Chromium fork tooling up front.
